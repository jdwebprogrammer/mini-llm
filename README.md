# MiniLLM

Simple LLM transformer from scratch (pytorch) with vocab built from your single 'input.txt' training text.

Since it combines a simple custom RAG, you can turn hyperparams down further (~16k) and still make related coherent sentences after only 10-50 epochs.
